<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Civireport" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/my_primary</item>
        <item name="colorPrimaryVariant">@color/my_primary</item>

        <item name="colorSecondary">@color/my_secondary</item>
        <item name="android:statusBarColor">@color/my_primary</item>

        <!-- Force visible blinking cursor/caret -->
        <item name="android:textCursorDrawable">@drawable/cursor</item>
        <item name="colorControlActivated">#0047AB</item>
    </style>

    <style name="Theme.Civireport" parent="Base.Theme.Civireport" />
</resources>